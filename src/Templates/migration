<?php


use Baseons\Database\Migration\Scheme\Scheme;
use Baseons\Database\Migration\Scheme\Table;

return new class
{
    public string|null $connection = null;

    public function up()
    {
        Scheme::table({{ $table ? "'" . strtolower($table) . "'" : "''" }}, function (Table $table) {
            $table->id();

            $table->dates();
        });
    }

    public function down()
    {
        {{ $table ? '' : '// '}}Scheme::dropTable('{{ strtolower($table ?? '' ) }}');
    }
};
